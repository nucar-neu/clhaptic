#summary Using profiling capabilities

= Introduction =

The application does require source code changes.

However an interceptor style system is presently in the works

= Details =

{{{
EventList * profiler = new EventList;
profiler->add();

}}}
